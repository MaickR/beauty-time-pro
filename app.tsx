// @ts-nocheck
/**
 * Archivo legacy — Beauty Time Pro
 *
 * Todo el contenido de este archivo ha sido migrado a src/caracteristicas/
 * durante la Fase 4 de refactorización. Este archivo se mantiene únicamente
 * como referencia histórica.
 *
 * Rutas migradas:
 * - Autenticación → src/caracteristicas/autenticacion/
 * - Maestro        → src/caracteristicas/maestro/
 * - Estudio        → src/caracteristicas/estudio/
 * - Reservas       → src/caracteristicas/reserva/
 *
 * Ejecutar: npm run semilla — para poblar datos de demostración (solo dev).
 */

export default function App() {
  return null;
}
